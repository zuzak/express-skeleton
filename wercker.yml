box: wercker/nodejs
build:
    steps:
        - script:
            name: "install application"
            code: "npm install"
        - script:
            name: "start application"
            code: "npm start &"
        - npm-test
