box: zuzak/nodebox 
build:
    steps:
        - script:
            name: "install application"
            code: "npm install"
        - script:
            name: "start application"
            code: "npm start &"
        - script:
            name: "test application"
            code: "npm test"
